kind: ConfigMap
apiVersion: v1
metadata:
  name: db-config
data:
  DB_DIALECT: org.hibernate.dialect.PostgreSQLDialect
  DB_DRIVER: org.postgresql.Driver
  DB_URL: 'jdbc:postgresql://external-postgres:15432/postgres'
  DB_USER: postgres
